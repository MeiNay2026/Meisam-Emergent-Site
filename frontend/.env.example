# Base URL of the deployed backend API (no trailing slash), e.g.:
# https://doctormeisam-api.onrender.com
# NOTE: renamed from REACT_APP_BACKEND_URL (Create React App) to NEXT_PUBLIC_BACKEND_URL
# (Next.js) as part of the migration off Create React App. Update this same
# variable name in Vercel's Environment Variables settings.
NEXT_PUBLIC_BACKEND_URL=
